# Restored Portfolio (cleaned)

This is a restored, cleaned version of your Vite + React + TypeScript portfolio.
It includes the Career Conversation project entry you confirmed.

Quick start:

npm install
npm run dev        # start dev server
npm run build      # build for production
npm run deploy     # deploy to gh-pages (requires gh-pages installed)
